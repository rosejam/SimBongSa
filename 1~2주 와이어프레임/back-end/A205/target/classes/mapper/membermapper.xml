<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.a205.model.membermapper">
 	<select id="select" resultType="member" parameterType="string">
		select * from `member` 
		<if test ="value!=null">
			where id = #{id}
		</if>
	</select>
	
	<update id="update" parameterType="member">
		update `member` set password=#{password}, name=#{name}, email=#{email}, 
							phone=#{phone}, address=#{address} where id=#{id} 
	</update>
	
	<delete id="delete" parameterType="string">
		delete from `member` where id=#{id}
	</delete>
	
	<insert id="insert" parameterType="member">
		insert into `member`(id,password,name,email,phone,address) 
		  values(#{id},#{password},#{name},#{email},#{phone},#{address} )
	</insert>
</mapper>